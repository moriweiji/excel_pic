# excel-pic

A minimal starter project for the episode Excel generator.

## Environment

- Package manager: `uv`
- Python: `>=3.11`

## Run

```bash
uv run excel-pic
```

## Notes

- This repository intentionally excludes local analysis docs and sample assets.
- Runtime data should be written to local `data/` (ignored by git).
